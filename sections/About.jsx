'use client';

//Include the style and motion files using Framer 
import { motion, MotionConfig } from 'framer-motion';
import styles from '../styles';
import { slideIn, staggerContainer, textVariant } from '../utils/motion';

const About = () => (
  <section>
    About section
  </section>
);

export default About;
